# Bolt's Journal

## 2024-05-22 - Firebase Bundle Size **Learning:** The Firebase SDK is a massive contributor to the bundle size (~677kB). **Action:** Always verify bundle composition. Future optimizations should look into code-splitting Firebase features or using lighter alternatives if possible, though strict architectural constraints often limit this.
